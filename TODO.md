# TODO

## Entry Path Planning

- Clarify what problem the future "quick-path entry" is meant to solve beyond the existing `Skip Checklist`.
- Decide whether `lightweight / standard / conflict-heavy` should become explicit entry tiers or stay as an internal framing only.
- Define trigger conditions for each tier without duplicating or weakening the current `Skip Checklist`.
- Define the minimum execution flow for each tier and verify it does not conflict with the judgment-first default flow.
- Decide where the entry-path guidance should live:
  - `SKILL.md`
  - a dedicated rule file
  - a separate reference document
- Decide whether each tier needs one small illustrative example.
- Check whether entry-path branching would weaken the current "judge first, then implement" identity.
- If adopted, do one terminology and navigation pass so the skill does not end up with two competing entry models.
